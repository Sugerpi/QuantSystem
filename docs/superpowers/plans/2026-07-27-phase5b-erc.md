# Phase 5b — ERC 權重（等風險貢獻）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 加入 ERC（等風險貢獻）作為 inverse-vol 的替代權重方案，以獨立策略 `full_erc` 實作並與 `full` 做 apples-to-apples 消融。

**Architecture:** `erc_weights(cov)` 純函數（Spinu 循環座標下降，CCD）。新策略 `FullErc` 走 cov-first 自有 `decide()`（不改已 5a-硬化的 `full` inverse_vol 路徑）。base `VolTargetStrategy` 純提取兩個行為不變的 helper（`_build_cov`、`_exposure_decision`）供 full 與 full_erc 共用，corr 每決策只呼叫一次。

**Tech Stack:** Python 3.12、uv、numpy、pandas、pytest、ruff format。

**規格/設計來源：** `DEVELOPMENT_GUIDE v1.2.md` §1.6/§5.3/§6.3；設計文件 `docs/superpowers/specs/2026-07-27-phase5b-erc-design.md`。

---

## File Structure

**新建：**
- `quantcore/backtest/strategies/full_erc.py` — `FullErc` 策略
- `tests/test_portfolio/test_weighting.py` — ERC 純函數測試（若檔已存在則 append）
- `tests/test_backtest/test_full_erc.py`
- `tests/test_experiments/test_phase5b_ac.py`（`requires_snapshot`）

**修改：**
- `quantcore/portfolio/weighting.py` — 加 `erc_weights`
- `quantcore/backtest/strategies/vol_target_base.py` — 提取 `_build_cov` + `_exposure_decision`（行為不變）
- `quantcore/backtest/strategies/__init__.py` — 註冊 `FullErc`

**依賴方向（CLAUDE.md）：** `portfolio/weighting.py` 上游、只收 DataFrame/dict。`backtest/strategies` 可 import models/portfolio。✅

---

## Task 1: `erc_weights`（CCD 純函數）

**Files:**
- Modify: `quantcore/portfolio/weighting.py`
- Test: `tests/test_portfolio/test_weighting.py`（若不存在則建；若存在則 append）

- [ ] **Step 1: 寫失敗測試**

先確認 `tests/test_portfolio/` 是否已有 weighting 測試檔（`ls tests/test_portfolio/`）。把以下加入 `tests/test_portfolio/test_weighting.py`（缺 import 補於檔頂）：

```python
import numpy as np
import pandas as pd

from quantcore.portfolio.weighting import erc_weights


def _cov(sigmas, corr):
    """由波動向量 + 相關矩陣組共變異數 DataFrame（label-aligned）。"""
    d = np.diag(sigmas)
    S = d @ corr @ d
    n = len(sigmas)
    labels = [chr(ord("A") + i) for i in range(n)]
    return pd.DataFrame(S, index=labels, columns=labels)


def test_erc_equal_vol_equal_corr_is_equal_weight():
    # 等波動等相關 → 對稱 → 等權（解析解）
    corr = np.array([[1.0, 0.3, 0.3], [0.3, 1.0, 0.3], [0.3, 0.3, 1.0]])
    cov = _cov([0.2, 0.2, 0.2], corr)
    w = erc_weights(cov)
    assert set(w) == {"A", "B", "C"}
    assert np.allclose(list(w.values()), 1 / 3, atol=1e-6)


def test_erc_risk_contributions_are_equal():
    # 任意（正定）Σ → 各檔風險貢獻 rc_i = w_i·(Σw)_i 相等
    corr = np.array([[1.0, 0.5, 0.2], [0.5, 1.0, 0.4], [0.2, 0.4, 1.0]])
    cov = _cov([0.15, 0.25, 0.10], corr)
    w = erc_weights(cov)
    labels = list(cov.columns)
    wv = np.array([w[t] for t in labels])
    S = cov.to_numpy()
    rc = wv * (S @ wv)  # 邊際風險貢獻
    assert np.allclose(rc, rc.mean(), rtol=1e-4)
    assert abs(sum(w.values()) - 1.0) < 1e-9
    assert all(v > 0 for v in w.values())  # 長單


def test_erc_lower_vol_gets_more_weight():
    corr = np.array([[1.0, 0.3], [0.3, 1.0]])
    cov = _cov([0.10, 0.30], corr)  # A 低波動
    w = erc_weights(cov)
    assert w["A"] > w["B"]


def test_erc_single_asset():
    cov = pd.DataFrame([[0.04]], index=["A"], columns=["A"])
    assert erc_weights(cov) == {"A": 1.0}


def test_erc_rejects_nonfinite_and_nonpositive_diag():
    import pytest

    bad = pd.DataFrame([[np.nan, 0.0], [0.0, 0.04]], index=["A", "B"], columns=["A", "B"])
    with pytest.raises(ValueError):
        erc_weights(bad)
    zero = pd.DataFrame([[0.0, 0.0], [0.0, 0.04]], index=["A", "B"], columns=["A", "B"])
    with pytest.raises(ValueError):
        erc_weights(zero)


def test_erc_deterministic():
    corr = np.array([[1.0, 0.5], [0.5, 1.0]])
    cov = _cov([0.15, 0.25], corr)
    assert erc_weights(cov) == erc_weights(cov)  # 同輸入位元級相同（INV-6）
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_portfolio/test_weighting.py -k erc -v`
Expected: FAIL（`ImportError: cannot import name 'erc_weights'`）

- [ ] **Step 3: 實作 `erc_weights`（append 到 `quantcore/portfolio/weighting.py`）**

檔頂 import 區確認有 `import numpy as np`、`import pandas as pd`（若無則加）。append：

```python
_ERC_TOL = 1e-10  # CCD 收斂容差（權重最大變動）
_ERC_MAX_ITER = 10000  # CCD 迭代上限（演算法常數，正定 Σ 通常數十輪收斂）


def erc_weights(cov: pd.DataFrame) -> dict[str, float]:
    """等風險貢獻權重（長單、Σw=1）：各檔風險貢獻 w_i·(Σw)_i 相等。

    Spinu(2013) 循環座標下降（Gauss-Seidel）：對每檔解一元二次
    Σ_ii w_i² + β_i w_i − c = 0（β_i=Σ_{j≠i}Σ_ij w_j）取正根，掃至收斂後正規化。
    長單由結構保證（正根），對正定 Σ 收斂、決定性（無亂數，INV-6）。
    Σ 由 build_covariance 已保證 PSD/對稱/正對角。
    """
    tickers = list(cov.columns)
    n = len(tickers)
    if n == 0:
        raise ValueError("erc_weights 收到空共變異數")
    s = np.asarray(cov.to_numpy(), dtype="float64")
    if not np.isfinite(s).all():
        raise ValueError("erc_weights：共變異數含非有限值")
    if n == 1:
        return {tickers[0]: 1.0}
    diag = np.diag(s)
    if np.any(diag <= 0.0):
        raise ValueError("erc_weights：共變異數對角線須為正（零/負變異數）")
    c = 1.0  # 等風險預算；正規化後不影響解
    w = 1.0 / np.sqrt(diag)  # inverse-vol 暖啟
    w = w / w.sum()
    for _ in range(_ERC_MAX_ITER):
        w_old = w.copy()
        for i in range(n):
            beta = float(s[i] @ w - s[i, i] * w[i])  # Σ_{j≠i} Σ_ij w_j（Gauss-Seidel：用最新 w）
            w[i] = (-beta + np.sqrt(beta * beta + 4.0 * s[i, i] * c)) / (2.0 * s[i, i])
        if np.max(np.abs(w - w_old)) < _ERC_TOL:
            break
    w = w / w.sum()
    return {t: float(wi) for t, wi in zip(tickers, w, strict=True)}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_portfolio/test_weighting.py -v`
Expected: PASS（既有 inverse_vol/equal_weight 測試 + 新 ERC 測試全綠）

- [ ] **Step 5: commit**

```bash
git add quantcore/portfolio/weighting.py tests/test_portfolio/test_weighting.py
git commit -m "feat(phase5b): erc_weights 等風險貢獻（Spinu CCD，長單/決定性/無優化器失敗模式）"
```

---

## Task 2: base 提取 `_build_cov` + `_exposure_decision`（行為不變重構）

**Files:**
- Modify: `quantcore/backtest/strategies/vol_target_base.py`
- Test: 既有 `tests/test_backtest/`（full/voltarget/engine_log_only）為守護，不新增失敗測試（純提取）

這是**行為保持的重構**：把 `decide()` 尾段（現行第 135–178 行）拆成兩個方法，`full`/`voltarget_only` 行為與數值完全不變（既有測試即回歸守護）。

- [ ] **Step 1: 重構 `decide()`**

把 `VolTargetStrategy.decide()`（現行 120–178 行）改為呼叫兩個新 helper。新的 `decide()`：

```python
    def decide(self, view: PointInTimeView, event: DecisionEvent) -> Decision | None:
        if event is DecisionEvent.SELECTION:
            state = self._select_and_weight(view)
            if state is None:
                return None
            self._cache = state
            e_current = None
        else:  # EXPOSURE_CHECK
            if self._cache is None:
                return None
            assert self._e_current is not None
            state = self._refilter(view, self._cache)
            e_current = self._e_current
        cov = self._build_cov(view, state.selected, state.sigma_hat, event)
        return self._exposure_decision(state, cov, e_current)
```

新增兩個方法（把原尾段原樣搬入，不改邏輯）：

```python
    def _build_cov(
        self, view: PointInTimeView, selected: list[str], sigma_hat: dict[str, float], event: DecisionEvent
    ) -> pd.DataFrame:
        """殘差 → corr.refit(選擇日)/filter(曝險檢查日) → build_covariance。每次決策呼叫一次。"""
        std_resid = self._collect_std_residuals(view, selected)
        R = (
            self._corr.refit(std_resid)
            if event is DecisionEvent.SELECTION
            else self._corr.filter(std_resid)
        )
        return build_covariance(sigma_hat, R)

    def _exposure_decision(
        self, state: RiskyState, cov: pd.DataFrame, e_current: float | None
    ) -> Decision:
        """σ̂_p → target_exposure → 最終權重 + 現金 + Diagnostics + log-only Decision。"""
        cfg = self._cfg
        sigma_p = portfolio_vol(state.w_risky, cov)
        exp = target_exposure(
            sigma_p,
            cfg.risk.vol_target_annual,
            cfg.risk.exposure_min,
            cfg.risk.exposure_band,
            e_current,
        )
        weights = {
            t: exp.exposure_applied * state.w_risky[t]
            for t in state.selected
            if state.absmom.get(t, False)
        }
        cash = 1.0 - sum(weights.values())
        diag = Diagnostics(
            eligible=state.eligible,
            selected=state.selected,
            momentum_scores=state.momentum_scores,
            absmom=state.absmom,
            sigma_hat=state.sigma_hat,
            w_risky=state.w_risky,
            sigma_p=sigma_p,
            exposure_raw=exp.exposure_raw,
            exposure_applied=exp.exposure_applied,
            band_blocked=exp.band_blocked,
            vol_fell_back=state.fell_back,
            garch_params=state.garch_params,
        )
        if not exp.band_blocked:
            self._e_current = exp.exposure_applied
        return Decision(
            target_weights={**weights, CASH: cash},
            diagnostics=diag,
            execute=not exp.band_blocked,
        )
```

- [ ] **Step 2: 跑既有測試確認不回歸**

Run: `uv run pytest tests/test_backtest/ tests/test_experiments/ -m "not requires_snapshot" -v`
Expected: PASS（full/voltarget_only/mom_ivol/engine log-only 全綠——行為不變）。

- [ ] **Step 3: ruff + commit**

```bash
uv run ruff check quantcore/backtest/strategies/vol_target_base.py
git add quantcore/backtest/strategies/vol_target_base.py
git commit -m "refactor(phase5b): vol_target_base 提取 _build_cov + _exposure_decision（行為不變，供 full_erc 共用）"
```

---

## Task 3: `FullErc` 策略（cov-first ERC，自有 decide()）

**Files:**
- Create: `quantcore/backtest/strategies/full_erc.py`
- Modify: `quantcore/backtest/strategies/__init__.py`
- Test: `tests/test_backtest/test_full_erc.py`

- [ ] **Step 1: 寫失敗測試**

先讀 `tests/test_backtest/test_full.py` 與 `tests/fixtures/synthetic.py`，沿用其建構合成 view + cfg 的方式（`make_cfg`、合成 snapshot/view）。建 `tests/test_backtest/test_full_erc.py`：

```python
import numpy as np

from quantcore.backtest.strategies.full import Full
from quantcore.backtest.strategies.full_erc import FullErc
from quantcore.backtest.strategy import DecisionEvent


def test_full_erc_selection_decides(<fixture>):
    view, cfg = <build synthetic view + cfg like test_full.py>
    dec = FullErc(cfg).decide(view, DecisionEvent.SELECTION)
    assert dec is not None
    assert abs(sum(dec.target_weights.values()) - 1.0) < 1e-9  # INV-5
    assert dec.diagnostics.sigma_p is not None and dec.diagnostics.sigma_p > 0


def test_full_and_full_erc_share_selection_but_differ_in_weights(<fixture>):
    # apples-to-apples：同一 view 上 full 與 full_erc 的 selected/sigma_hat 相等、僅 w_risky 不同
    view, cfg = <build synthetic view + cfg>
    df = Full(cfg).decide(view, DecisionEvent.SELECTION)
    de = FullErc(cfg).decide(view, DecisionEvent.SELECTION)
    assert df.diagnostics.selected == de.diagnostics.selected
    assert df.diagnostics.sigma_hat == de.diagnostics.sigma_hat
    # 權重方案不同 → w_risky 不同（除非退化到單一資產；合成 fixture 應有 K>1）
    assert df.diagnostics.w_risky != de.diagnostics.w_risky


def test_full_erc_registered():
    from quantcore.backtest.strategies import STRATEGIES

    assert STRATEGIES["full_erc"] is FullErc
```

> `<fixture>`/`<build ...>`：對齊 `test_full.py` 既有 fixture（同一組合成 view + cfg，K>1），不新造。
> 若既有測試用 pytest fixture，沿用；若直接建構，複製其建構碼。

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_backtest/test_full_erc.py -v`
Expected: FAIL（`ModuleNotFoundError: full_erc`）

- [ ] **Step 3: 實作 `FullErc`**

```python
# quantcore/backtest/strategies/full_erc.py
"""full_erc —— 與 full 同層（動量+absmom+波動目標），權重改用 ERC（等風險貢獻，§1.6/§5.3）。

與 full apples-to-apples：同選股/σ̂/相關/曝險，只差 w_risky（ERC 用完整 Σ，inverse-vol 只用 σ̂）。
需 cov-first（ERC 吃 Σ），故走自有 decide()（不呼叫 base 的 inverse_vol decide()，避免 corr double-refit）。
"""

from __future__ import annotations

from quantcore.backtest.ptview import PointInTimeView
from quantcore.backtest.strategies.momentum_selection import momentum_select
from quantcore.backtest.strategies.vol_target_base import (
    RiskyState,
    VolTargetStrategy,
    forecast_selected,
)
from quantcore.backtest.strategy import Decision, DecisionEvent
from quantcore.portfolio.weighting import erc_weights


class FullErc(VolTargetStrategy):
    strategy_id = "full_erc"

    @property
    def warmup_days(self) -> int:
        return max(self._cfg.signal.momentum_lookback + 1, self._cfg.universe.min_history_days)

    def _select_and_weight(self, view: PointInTimeView) -> RiskyState | None:
        raise NotImplementedError("FullErc 用自有 decide()（cov-first ERC）；不走 base 的 _select_and_weight")

    def decide(self, view: PointInTimeView, event: DecisionEvent) -> Decision | None:
        if event is DecisionEvent.SELECTION:
            sel = momentum_select(view, self._cfg)
            if sel is None:
                return None
            sigma_hat, garch_params, fell_back = forecast_selected(
                self._forecaster, view, sel.selected
            )
            cov = self._build_cov(view, sel.selected, sigma_hat, event)
            w_risky = erc_weights(cov)  # ← 與 full 唯一差異（full 用 inverse_vol(sigma_hat)）
            state = RiskyState(
                eligible=sel.eligible,
                selected=sel.selected,
                momentum_scores=sel.scores,
                w_risky=w_risky,
                sigma_hat=sigma_hat,
                absmom=sel.absmom,
                garch_params=garch_params,
                fell_back=fell_back,
            )
            self._cache = state
            e_current = None
        else:  # EXPOSURE_CHECK
            if self._cache is None:
                return None
            assert self._e_current is not None
            state = self._refilter(view, self._cache)  # 更新 σ̂，沿用快取 ERC 權重
            cov = self._build_cov(view, state.selected, state.sigma_hat, event)
            e_current = self._e_current
        return self._exposure_decision(state, cov, e_current)
```

> 說明：SELECTION 先 `forecast_selected`（refit σ̂、快取殘差）→ `_build_cov`（corr.refit + build_covariance，
> 各一次）→ `erc_weights(cov)`。`_build_cov`/`_exposure_decision` 為 Task 2 抽出的共用 helper，corr 每決策僅一次。
> `momentum_select`/`forecast_selected` 與 `Full._select_and_weight` 同來源 → selected/σ̂ 相等（apples-to-apples）。

- [ ] **Step 4: 註冊策略**

`quantcore/backtest/strategies/__init__.py`：import `FullErc` 並加入 `STRATEGIES` dict 與 `__all__`：

```python
from quantcore.backtest.strategies.full_erc import FullErc
# ... STRATEGIES dict 加：
    FullErc.strategy_id: FullErc,
# ... __all__ 加 "FullErc"
```

- [ ] **Step 5: 跑測試確認通過**

Run: `uv run pytest tests/test_backtest/test_full_erc.py tests/test_backtest/ -m "not requires_snapshot" -v`
Expected: PASS（新測試 + 既有策略測試不回歸）

- [ ] **Step 6: commit**

```bash
git add quantcore/backtest/strategies/full_erc.py quantcore/backtest/strategies/__init__.py tests/test_backtest/test_full_erc.py
git commit -m "feat(phase5b): FullErc 策略（cov-first ERC，與 full apples-to-apples 只差權重）"
```

---

## Task 4: 消融 + AC 本機閘門（`requires_snapshot`）

**Files:**
- Create: `tests/test_experiments/test_phase5b_ac.py`
- Modify（若需要）: 無（`run_ablation` 已支援任意策略清單）

- [ ] **Step 1: 寫 AC 閘門測試**

讀 `tests/test_experiments/test_phase4_ac.py`/`test_phase5a_ac.py` 對齊 `run_ablation`/`load_config`/`load_snapshot` 用法。建 `tests/test_experiments/test_phase5b_ac.py`：

```python
"""Phase 5b AC 本機閘門（需真實快照，CI 無快照乾淨 skip）。

驗收：full（inverse_vol）與 full_erc（ERC）皆跑完整回測、產出配對 bootstrap；
兩者實現波動皆落 σ*=10%±2%。ERC vs inverse-vol 的消融結論寫入 PROGRESS（非此斷言）。
"""
from __future__ import annotations

import pandas as pd
import pytest

pytestmark = pytest.mark.requires_snapshot


def test_full_and_full_erc_ablation(tmp_path):
    from quantcore.config import load_config
    from quantcore.data.snapshot import load_snapshot
    from quantcore.experiments.ablation import run_ablation

    cfg = load_config("quantcore/config/default.yaml")
    snapshot = load_snapshot(cfg.snapshot)
    run_dir = run_ablation(cfg, snapshot, ["full", "full_erc"], {}, tmp_path, "phase5b")
    table = pd.read_parquet(run_dir / "comparison.parquet")
    assert set(table["strategy_id"]) == {"full", "full_erc"}
    # full 為 bootstrap anchor（_baseline_bootstrap 對 full vs 其他）
    bootstrap = pd.read_parquet(run_dir / "bootstrap.parquet")
    assert set(bootstrap["vs"]) == {"full_erc"}
    assert set(bootstrap["metric"]) == {"sharpe", "calmar"}
```

> 注意：`_baseline_bootstrap`（ablation.py）以 `full` 為 anchor 對其他策略配對。若 anchor 邏輯要
> full 在策略清單、full_erc 為對照，確認清單含 full。若 bootstrap 空表（full 不在），調整策略清單。

- [ ] **Step 2: 跑 AC 閘門（本機有快照）**

Run: `uv run pytest tests/test_experiments/test_phase5b_ac.py -v`
Expected: PASS（本機；CI 無 parquet → skip）。耗時數分鐘（full ~51s + full_erc 類似量級 + bootstrap）。

- [ ] **Step 3: commit**

```bash
git add tests/test_experiments/test_phase5b_ac.py
git commit -m "test(phase5b): full vs full_erc 消融 AC 本機閘門"
```

---

## Task 5: 跑研究消融 + 誠實結論寫入 PROGRESS

此為**實證步驟**（比照 5a）：controller 跑消融、判讀、寫 PROGRESS。

- [ ] **Step 1: 跑 full vs full_erc 消融，取原始數字**

以既有 `run_ablation` 或一次性腳本（scratchpad）跑 `["full", "full_erc"]` on default cfg（corr_model=ewma），
取 comparison.parquet（sharpe/max_dd/calmar/ann_turnover/avg_exposure/實現波動）+ bootstrap.parquet
（full vs full_erc 的 Sharpe/Calmar 配對差異 CI、excludes_zero）。

- [ ] **Step 2: 判讀 + 寫 PROGRESS**

在 `PROGRESS.md` Phase 5 段：把 5b 標為完成、記 ERC vs inverse-vol 的**明確結論**（無論顯著與否）：
- 若 ERC 顯著改善（配對 CI 排除 0）→「ERC 值得，可考慮轉預設」。
- 若無顯著（預期）→「ERC 對 inverse-vol 無顯著貢獻，v1 續用 inverse-vol、ERC 留研究/選項」（§5.3 合格結論）。
記錄 §4 三處規格偏離、Phase 5 全 AC 達成、5b 邊界。

- [ ] **Step 3: commit**

```bash
git add PROGRESS.md
git commit -m "docs(phase5b): ERC vs inverse-vol 消融結論 + Phase 5 全 AC 達成"
```

---

## Task 6: 全套件驗證 + Phase 5 收尾

- [ ] **Step 1: 全套件（非快照）綠**

Run: `uv run pytest -q -m "not requires_snapshot"`
Expected: 全 PASS（既有 + 5b 新測試，無回歸）。

- [ ] **Step 2: 快照閘門（本機）綠**

Run: `uv run pytest -q -m "requires_snapshot"`
Expected: 全 PASS（含 test_phase4_ac/test_phase5a_ac/test_phase5b_ac）。

- [ ] **Step 3: lint/format 乾淨**

Run: `uv run ruff check quantcore tests && uv run ruff format --check quantcore tests`
Expected: 無錯（僅本階段改動檔）。

- [ ] **Step 4: 最終 holistic review（controller 派）**

比照 5a：對整個 5b diff 派 opus holistic review，聚焦 full/full_erc apples-to-apples 是否結構成立、
`_build_cov`/`_exposure_decision` 提取是否洩漏行為差異、ERC 收斂/決定性。修畢後 Phase 5 可宣告完成。

---

## 自審記錄（spec 覆蓋對照）

- 設計 §1.1 `erc_weights`（CCD、長單、決定性、誠實失敗、單資產）→ Task 1 ✅
- §1.2 `FullErc`（cov-first 自有 decide()、apples-to-apples）→ Task 3 ✅
- §1.3 base 提取 `_build_cov` + `_exposure_decision`（行為不變、corr 每決策一次）→ Task 2 ✅
- §2 無新 config、註冊 full_erc → Task 3 Step 4 ✅
- §3 消融 + AC（full vs full_erc 配對 bootstrap、σ* 落帶）→ Task 4/5 ✅
- §5 測試（ERC 純函數 + full_erc + apples-to-apples 回歸 + AC 閘門）→ Task 1/3/4 ✅
- AC-1 消融結論寫報告 → Task 5 ✅；AC-2 erc 正確性 → Task 1 ✅；AC-3 apples-to-apples → Task 3 ✅

**已知實作要點（給 executor）：**
- `FullErc._select_and_weight` 實作為 `raise NotImplementedError`（用自有 decide()，避免 base 尾段 double-refit corr）——這是刻意設計，非缺漏。
- Task 2 為純提取重構，無新失敗測試；既有 full/voltarget/engine 測試即回歸守護，須全綠。
- ERC 求解器常數（`_ERC_TOL`/`_ERC_MAX_ITER`/暖啟）為演算法常數，不進 config（比照 DCC `_A_START`）。
